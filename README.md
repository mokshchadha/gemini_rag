# gemini_rag
Gemini Rag for processing PDFs and then querying them later 


## steps to improve the provided pdfs
1. reduce the size of very large pdfs -- easy to be sent to the LLM (if the PDF is over 75MB with HD images please compress)
2. PPT when converted to PDF need to refactored into simple PDFs that can be text only -- trim the pdfs to keep relevant info rather then un required images